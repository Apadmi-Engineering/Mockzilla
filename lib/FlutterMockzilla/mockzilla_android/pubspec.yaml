name: mockzilla_android
description: A new Flutter plugin project.
version: 0.0.1

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.3.0'

flutter:
  plugin:
    implements: mockzilla
    platforms:
      android:
        dartPluginClass: MockzillaAndroid
        package: com.apadmi.mockzilla
        pluginClass: MockzillaAndroidPlugin

dependencies:
  flutter:
    sdk: flutter
  mockzilla_platform_interface:
    path: ../mockzilla_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
  pigeon: ^12.0.1
  flutter_lints: ^2.0.0
